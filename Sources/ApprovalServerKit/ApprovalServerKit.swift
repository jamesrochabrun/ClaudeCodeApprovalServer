import Foundation

/// ApprovalServerKit provides shared types and utilities for the Approval MCP Server
public struct ApprovalServerKit {
  /// The current version of the ApprovalServerKit
  public static let version = "1.0.0"

  /// Initialize the kit
  public init() {}
}